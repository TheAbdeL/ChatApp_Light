name: chatapp_light
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  
  # Firebase
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  firebase_storage: ^13.0.5
  
  # State Management
  provider: ^6.1.1
  
  # Images
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  
  # Audio - Messages Vocaux ✅ NOUVELLE APPROCHE
  flutter_sound: ^9.2.13        # ← Package stable pour étudiant
  permission_handler: ^11.3.1
  path_provider: ^2.1.4
  
  # Utilitaires
  intl: ^0.20.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true